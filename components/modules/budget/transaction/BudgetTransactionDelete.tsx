import { BudgetTransactionType } from "@/types/modules/budget/budget-transaction-types";

const BudgetTransactionDelete = ({transaction}:{transaction:BudgetTransactionType}) => {
    return (
        <>
            
        </>
    );
};

export default BudgetTransactionDelete;
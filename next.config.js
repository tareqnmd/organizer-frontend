/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'www.gravatar.com',
			'github.com',
			'avatars.githubusercontent.com',
		],
	},
};

module.exports = nextConfig;
